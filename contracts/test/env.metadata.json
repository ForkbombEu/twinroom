{
    "description": "Example of a command that can use env variables",
    "options": [
        {
            "name": "-c, --content <string>",
            "description": "Pass a string as content",
            "env": [
                "FILE_CONTENT"
            ]
        }

    ]
}
